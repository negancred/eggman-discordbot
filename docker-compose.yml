services:
  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:latest
    container_name: lavalink
    restart: unless-stopped
    volumes:
      - ./lavalink/application.yml:/opt/Lavalink/application.yml
    ports:
      - "2333:2333"

  eggman:
    build: .
    container_name: eggman
    restart: unless-stopped
    depends_on:
      - lavalink
    env_file:
      - .env
